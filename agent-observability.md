complete
- Ack
  - Implemented the missing user-visible observability presentation in the isolated worktree at `.claude/worktrees/cli-session-observability` using baseline `92d57ed`.
  - Identified the active user-visible listing surface as the MCP-facing `list-sessions` output in `src/index.ts`.
  - Reused shared session-service data and `consumeDeadSessionNotices()` instead of duplicating observability logic.
- Files
  - `.claude/worktrees/cli-session-observability/src/index.ts` - added shared presentation helpers for notices and session summaries; updated `list-sessions`, `start-session`, `exec`, `close-session`, and `execSshCommand` to surface dead-session notices and metadata.
  - `.claude/worktrees/cli-session-observability/test/session-service.test.ts` - added focused tests for notice prepending, dead-session list formatting, empty-state formatting, and summary fields including `status`, `reason`, `logPath`, and `detectedAt`.
- Validation
  - Commands run:
    - `pwd`
    - `ls -la`
    - `ls -la "/home/k10876/ssh-cli-sessions/.claude/worktrees"`
    - `git -C "/home/k10876/ssh-cli-sessions/.claude/worktrees/cli-session-observability" status --short`
    - `git -C "/home/k10876/ssh-cli-sessions/.claude/worktrees/cli-session-observability" rev-parse --short HEAD`
    - `git -C "/home/k10876/ssh-cli-sessions/.claude/worktrees/cli-session-observability" diff -- src/index.ts test/session-service.test.ts`
    - `cd "/home/k10876/ssh-cli-sessions/.claude/worktrees/cli-session-observability" && npm test -- test/session-service.test.ts`
    - `cd "/home/k10876/ssh-cli-sessions/.claude/worktrees/cli-session-observability" && npm run build`
    - `ls -la "/home/k10876/ssh-cli-sessions/.claude/worktrees/cli-session-observability/.trellis" && ls -la "/home/k10876/ssh-cli-sessions/.claude/worktrees/cli-session-observability/.trellis/workspace" && ls -la "/home/k10876/ssh-cli-sessions/.claude/worktrees/cli-session-observability/.trellis/workspace/k10876"`
  - Results:
    - Focused test file passed.
    - Worktree-local build passed.
    - Root `npm test` was not run.
- Next
  - Carry the same shared presentation helpers into the human CLI `list` / `ps` surface when that command layer is merged into this worktree.
  - Keep existing observability backend changes in `src/services/session-service.ts`, `src/types/dead-session.ts`, and `src/types/session.ts` as the data source for future CLI presentation.
